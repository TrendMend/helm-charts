apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "retailer.fullname" . }}-test-connection"
  labels:
    {{- include "retailer.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "retailer.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
